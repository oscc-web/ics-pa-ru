<!-- # PA3 - 穿越时空的旅程: 批处理系统 -->
# PA3 - Путешествие сквозь пространство и время: Система пакетной обработки

<!-- > #### flag::世界诞生的故事 - 第三章
> 冯诺依曼计算机果然功力深厚, 竟然能向冷冰冰的门电路赋予新的生命.
> 为了让计算机自动运行一组程序, 先驱对计算机进行了改进. -->
> #### flag::История сотворения мира - Глава третья
> Фон-неймановский компьютер и правда обладает глубокими возможностями — он способен вдохнуть новую жизнь в холодные, безучастные логические вентили.
> Чтобы компьютер мог автоматически выполнять набор программ, первопроходцы усовершенствовали его архитектуру.

<!-- > #### danger::代码管理
> 在进行本PA前, 请在工程目录下执行以下命令进行分支整理, 否则将影响你的成绩:
```
git commit --allow-empty -am "before starting pa3"
git checkout master
git merge pa2
git checkout -b pa3
``` -->
> #### danger::Управление кодом
> Перед началом этой PA, пожалуйста, выполните в директории проекта следующие команды для упорядочивания веток — в противном случае это скажется на вашей оценке:
```
git commit --allow-empty -am "before starting pa3"
git checkout master
git merge pa2
git checkout -b pa3
```

<!-- > #### danger::提交要求(请认真阅读以下内容, 若有违反, 后果自负)
> **<u>预计平均耗时</u>**: 40小时
>
> **<u>阶段性安排</u>**:
> * task PA3.1: 实现自陷操作`yield()`
> * task PA3.2: 实现用户程序的加载和系统调用, 支撑TRM程序的运行
> * task PA3.3: 运行仙剑奇侠传并展示批处理系统, 提交完整的实验报告 -->
> #### danger::Требования к сдаче (пожалуйста, внимательно прочитайте следующее; в случае нарушения — ответственность лежит на вас)
> **<u>Ожидаемое среднее время</u>**: 40 часов
>
> **<u>Поэтапный план</u>**:
> * задача PA3.1: реализовать операцию самоловушки (trap) `yield()`
> * задача PA3.2: реализовать загрузку пользовательских программ и системные вызовы для обеспечения работы TRM-программ
> * задача PA3.3: запустить «Легенду о мече и фее» (仙剑奇侠传) и продемонстрировать систему пакетной обработки, сдать полный отчёт о лабораторной работе